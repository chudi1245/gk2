#!/usr/bin/perl
use strict;
use Genesis;
use FBI;

our $host = shift;
our $f    = new Genesis($host);
our $JOB  = $ENV{JOB};
our $STEP = $ENV{STEP};

kysy();
my $work_layer=get_work_layer();
if (not $work_layer) {
	p__('not work layer exit');
	exit;
}else{
	$f->COM ('sel_drawn',type=>'mixed',therm_analyze=>'no');
	exit;
}
































































































































































































































































































